{ Computed factorial in TINYLT }
read x ;
if x > 0 then { don’t compute otherwise }
  fact := 1;
  repeat
    fact := fact * x ;
    x := x - 1;
  until x == 0;
  write fact ; { output the computed factorial }
end { terminates the if stmt }